cmake_minimum_required(VERSION 3.0)
project( BlurImage )
find_package( OpenCV )
include_directories( ${OpenCV_INCLUDE_DIRS} )
add_executable( singleObject singleObject.cpp )
target_link_libraries( singleObject ${OpenCV_LIBS} )
